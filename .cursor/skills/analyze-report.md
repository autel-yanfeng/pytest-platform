# Skill: analyze-report

**触发词：** `分析测试报告` / `测试结果怎么样` / `为什么失败`

## 指令

调用 MCP test-platform 工具分析测试结果：

1. 调用 `get_last_report` 获取摘要
2. 调用 `get_failures` 获取失败列表
3. 调用 `get_trend` 获取趋势
4. 输出：
   - 一句话总结（通过率 + 耗时）
   - 失败用例表格（用例名 + 失败原因归类）
   - 趋势判断（变好/变差/稳定）
   - 针对失败给出最可能的修复方向
